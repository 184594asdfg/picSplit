<view class="container">
  <!-- 顶部导航栏 -->
  <view class="nav-bar">
    <view class="back-btn" bindtap="onBack">&lt;</view>
    <view class="page-title">图片已保存</view>
    <view class="window-controls">
      <view class="window-btn">•••</view>
      <view class="window-btn">—</view>
      <view class="window-btn">⦿</view>
    </view>
  </view>

  <!-- 功能按钮区 -->
  <view class="action-section">
    <view class="action-item" bindtap="onActionTap" data-action="grid">
      <view class="action-icon icon-grid">⌗</view>
      <view class="action-text">继续九宫格切图</view>
    </view>
    <view class="action-item" bindtap="onActionTap" data-action="collage">
      <view class="action-icon icon-collage">▣</view>
      <view class="action-text">自由拼图</view>
    </view>
    <view class="action-item" bindtap="onActionTap" data-action="split">
      <view class="action-icon icon-split">▦</view>
      <view class="action-text">分割图片</view>
    </view>
    <view class="action-item" bindtap="onActionTap" data-action="share">
      <view class="action-icon icon-share">⇧</view>
      <view class="action-text">分享给朋友</view>
    </view>
  </view>

  <!-- 下载顺序标题 -->
  <view class="order-title">下载后的顺序</view>
  <view class="expand-icon" bindtap="onToggleExpand">⌄</view>

  <!-- 宫格预览区 -->
  <view class="preview-grid">
    <view 
      wx:for="{{previewList}}" 
      wx:key="index"
      class="preview-item"
      bindtap="onPreviewTap"
      data-index="{{index}}"
    >
      <image 
        wx:if="{{item}}" 
        class="preview-image" 
        src="{{item}}" 
        mode="aspectFill"
      />
      <view class="order-number">{{index + 1}}</view>
    </view>
  </view>
</view>
